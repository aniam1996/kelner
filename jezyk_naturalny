

zamowienie= []
x=input()
zamowienie.append(x)



notatka = ",".join(zamowienie)

hasla = notatka.split(" ")
x=[]

for haslo in hasla:
    if haslo=="poprosze" or haslo=="prosze" or haslo=="," or haslo=="dziekuje" or haslo==" " or haslo=="." or haslo=="i" or haslo=="oraz" or haslo=="z":
        continue
    else:
        x.append(haslo)



print(x)
