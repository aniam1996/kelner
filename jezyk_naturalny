x=input()                           #wpisz zdanie
zamowienie= set(x.split())          #tworzenie zbioru wyrazów zdania

f=open('wykresl.txt','r').read()    #otwórz plik tekstowy z wyrazami do wykreślenia w trybie read
wykresl=set(f.split('\n'))          #tworzenie zbioru z hasłami do wykreślenia


hasla= zamowienie.difference(wykresl)  # zbiór wyrazów po wykreśleniu


print(hasla)

